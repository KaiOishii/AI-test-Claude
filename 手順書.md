# アプリ起動・テスト手順書

## 目次

1. [前提条件](#1-前提条件)
2. [リポジトリのセットアップ](#2-リポジトリのセットアップ)
3. [ToDoアプリ](#3-todoアプリ)
   - 起動手順
   - 手動テスト手順
   - 自動テスト（Playwright）
4. [WBS管理アプリ](#4-wbs管理アプリ)
   - 起動手順
   - 手動テスト手順（機能一覧）
5. [よくあるトラブルと対処法](#5-よくあるトラブルと対処法)

---

## 1. 前提条件

### 必須ソフトウェア

| ソフトウェア | バージョン | 確認コマンド |
|---|---|---|
| Node.js | 20.9 以上 | `node -v` |
| npm | 10 以上 | `npm -v` |
| Git | 任意 | `git --version` |

### インストール方法（未インストールの場合）

**Node.js のインストール**

- 公式サイト: https://nodejs.org/ja/
- LTS版（推奨）を選択してインストール
- インストール後、ターミナルを再起動して `node -v` で確認

**macOS の場合（Homebrew 使用）**

```bash
brew install node
```

**Windows の場合**

- 公式サイトからインストーラーをダウンロードして実行
- インストール時に「Add to PATH」にチェックを入れる

---

## 2. リポジトリのセットアップ

```bash
# リポジトリをクローン
git clone https://github.com/KaiOishii/AI-test-Claude.git
cd AI-test-Claude
```

リポジトリの構成：

```
AI-test-Claude/
├── todo-app/        ← ToDoアプリ（Next.js + SQLite）
├── wbs-app/         ← WBS管理アプリ（Next.js + SQLite）
└── 手順書.md
```

---

## 3. ToDoアプリ

### 3-1. 起動手順

```bash
# ディレクトリに移動
cd todo-app

# 依存パッケージをインストール（初回のみ）
npm install

# 開発サーバーを起動
npm run dev
```

起動成功時のメッセージ：

```
▲ Next.js 16.x.x
- Local:   http://localhost:3000
```

ブラウザで http://localhost:3000 を開く。

> **注意**: `.data/todos.db` が自動で作成されます（初回起動時）。このファイルは `.gitignore` に含まれているため、リポジトリには含まれません。

---

### 3-2. 手動テスト手順

#### アカウント登録とログイン

1. http://localhost:3000 にアクセス → `/login` にリダイレクトされることを確認
2. 「新規登録」リンクをクリック
3. 名前・メールアドレス・パスワードを入力して登録
4. `/todos` 画面に遷移し、右上に登録した名前が表示されることを確認
5. 「ログアウト」ボタンをクリック → `/login` に戻ることを確認
6. 再度ログイン → `/todos` に遷移することを確認

#### ToDoの基本操作

| 操作 | 手順 |
|---|---|
| **追加** | `+ 追加` ボタン → タイトル入力 → `追加` ボタン → リストに表示されることを確認 |
| **完了切り替え** | チェックボックス（左端の□）をクリック → タイトルに打ち消し線が入ることを確認。再クリックで元に戻ることを確認 |
| **編集** | 項目にマウスオーバー → `編集` ボタン → タイトルを変更 → `保存` → 変更後のタイトルが表示されることを確認 |
| **削除** | 項目にマウスオーバー → `削除` ボタン → 一覧から消えることを確認 |

#### 優先度設定

1. `+ 追加` → タイトルを入力
2. 優先度セレクトボックスで「高」を選択
3. `追加` → リスト項目に `高` バッジが表示されることを確認

#### 検索機能

1. いくつかToDoを追加しておく
2. 検索バーにキーワードを入力 → 該当するToDoだけ表示されることを確認
3. 検索バーをクリアすると全件表示に戻ることを確認

#### フィルター機能

| フィルター | 確認内容 |
|---|---|
| `すべて` | 全件表示 |
| `未完了` | 完了済みToDoが非表示になることを確認 |
| `完了済み` | 未完了ToDoが非表示になることを確認 |

#### カテゴリ管理

1. サイドバーの「カテゴリ」横の `+` をクリック
2. カテゴリ名を入力 → `追加`
3. サイドバーにカテゴリが表示されることを確認
4. ToDo追加フォームのカテゴリ選択に表示されることを確認

#### サブタスク

1. ToDoにマウスオーバー → `＋` ボタン（サブタスク追加）をクリック
2. タイトルを入力して追加
3. 親タスクに `▸ 0/1` のような展開ボタンが表示されることを確認
4. クリックしてサブタスクが表示されることを確認

#### ダークモード

1. ヘッダー右上の `☾`（月）ボタンをクリック
2. 画面が暗くなることを確認
3. ページをリロードしても暗いままであることを確認（localStorage に保存）
4. `☀`（太陽）ボタンをクリックしてライトモードに戻ることを確認

#### カレンダー表示

1. サイドバーの「カレンダー」をクリック
2. 月次カレンダーが表示されることを確認
3. `<` `>` ボタンで前月・翌月に移動できることを確認
4. 期限日が設定されたToDoがある場合、その日付に点が表示されることを確認

#### 繰り返しタスク

1. `+ 追加` → タイトルを入力
2. 繰り返しセレクトで「毎日」を選択
3. 期限日を本日に設定して追加
4. 完了にする → 翌日の期限で同じタイトルのToDoが自動生成されることを確認

---

### 3-3. 自動テスト（Playwright）

ToDoアプリには Playwright による E2E テストが実装されています。

#### 事前準備

```bash
cd todo-app

# 依存パッケージをインストール（未実施の場合）
npm install

# 開発サーバーを起動したまま別のターミナルを開く
# （または reuseExistingServer: true の設定により自動起動される）
```

#### テスト実行

```bash
# すべてのテストを実行
npx playwright test

# テスト結果を詳細表示
npx playwright test --reporter=list

# 特定のテストだけ実行（例：認証フロー）
npx playwright test --grep "認証フロー"

# ブラウザを表示しながら実行（デバッグ用）
npx playwright test --headed
```

#### テスト内容一覧

| テストグループ | テスト名 |
|---|---|
| 認証フロー | 未認証時は login にリダイレクト |
| 認証フロー | ログイン成功後にメイン画面へ遷移 |
| 認証フロー | ログアウト後に login 画面へ遷移 |
| ToDoの基本操作 | ToDoの追加 |
| ToDoの基本操作 | ToDoの完了切り替え |
| ToDoの基本操作 | ToDoの削除 |
| ToDoの基本操作 | ToDoの編集 |
| 検索機能 | タイトルで検索できる |
| 優先度機能 | 高優先度のToDoを追加できる |
| フィルター機能 | 未完了フィルターで完了済みが非表示 |
| カテゴリ管理 | カテゴリの追加 |
| ダークモード | ダークモード切り替えボタンが存在する |
| カレンダー表示 | カレンダービューに切り替えられる |

#### テストレポートの確認

```bash
# HTML レポートを生成・表示
npx playwright show-report
```

---

## 4. WBS管理アプリ

### 4-1. 起動手順

```bash
# ディレクトリに移動（AI-test-Claude の中から）
cd wbs-app

# 依存パッケージをインストール（初回のみ）
npm install

# 開発サーバーを起動
npm run dev
```

起動確認後、ブラウザで http://localhost:3000 を開く。

> **注意**: ToDoアプリと WBS アプリは両方とも port 3000 を使用します。同時に起動する場合は、片方を先に起動してから、もう一方を別のポートで起動してください。
>
> 別ポートで起動する方法：
> ```bash
> PORT=3001 npm run dev
> # または
> npx next dev -p 3001
> ```

---

### 4-2. 手動テスト手順

#### アカウント登録とログイン

1. http://localhost:3000 にアクセス → `/login` にリダイレクトされることを確認
2. 「新規登録」をクリック
3. 名前・メールアドレス・パスワード（6文字以上）を入力 → 「登録する」
4. `/dashboard`（ホーム画面）に遷移することを確認
5. サイドバーに名前が表示されることを確認

---

#### ホーム画面（ダッシュボード）

確認項目：

- [ ] 「今日のタスク」セクションが表示される
- [ ] 「期限切れ」セクションが表示される（期限切れタスクがあれば）
- [ ] 「進行中のプロジェクト」セクションが表示される
- [ ] サイドバーのナビゲーション（ホーム・プロジェクト・今日のタスク）が機能する

---

#### プロジェクト管理

**プロジェクトの作成**

1. サイドバー「プロジェクト」またはホーム画面の「すべて見る」をクリック
2. 右上の「+ 新規」ボタンをクリック
3. 以下を入力してテスト：
   - プロジェクト名: `テストプロジェクト`
   - 説明: `これはテスト用のプロジェクトです`
   - 開始日: 本日の日付
   - 終了予定日: 1ヶ月後の日付
   - ステータス: 進行中
   - 進捗率: 0
4. 「追加」ボタンをクリック
5. プロジェクト一覧に表示されることを確認

**プロジェクトの編集**

1. プロジェクト一覧で、項目にマウスオーバー → 「編集」をクリック
2. プロジェクト名や進捗率を変更 → 「保存」
3. 変更が反映されることを確認

**プロジェクトのフィルター**

1. プロジェクト一覧画面のフィルターボタン（すべて・進行中・完了・保留・中止）をクリック
2. 対応するステータスのプロジェクトのみ表示されることを確認

**プロジェクトの削除**

1. 項目にマウスオーバー → 「削除」
2. 確認ダイアログで OK → プロジェクトと配下のタスクが削除されることを確認

---

#### WBSタスク管理

**前提**: プロジェクトを1つ作成済みであること。

**ルートタスクの追加**

1. プロジェクト一覧からプロジェクト名をクリック → プロジェクト詳細画面
2. 「+ タスク追加」ボタンをクリック
3. 以下を入力：
   - タスク名: `要件定義`
   - ステータス: 未着手
   - 優先度: 高
   - 期限: 1週間後
   - 予定工数: 8
4. 「追加」をクリック
5. WBSリストに表示されることを確認

同様にもう1つルートタスクを追加：
- タスク名: `設計`
- ステータス: 未着手
- 優先度: 中

**子タスクの追加（WBS階層）**

1. `要件定義` タスクにマウスオーバー → 右端の「＋」ボタンをクリック
2. 子タスクのフォームが開く
3. タスク名: `画面要件の定義`、予定工数: 3 を入力して追加
4. `要件定義` の下に `▾` ボタンが表示され、その下に子タスクが表示されることを確認
5. 同様に `機能要件の定義`（予定工数: 5）も追加

**孫タスクの追加（3階層）**

1. `画面要件の定義` にマウスオーバー → 「＋」をクリック
2. タスク名: `ログイン画面設計` を追加
3. 3段インデントで表示されることを確認

**ツリーの展開・折りたたみ**

1. `▾` ボタンをクリック → 子タスクが非表示になることを確認（`▸` に変わる）
2. `▸` をクリック → 子タスクが再表示されることを確認

**タスクの編集**

1. タスクにマウスオーバー → 「編集」をクリック
2. タスク名・ステータス・進捗率などを変更 → 「保存」
3. 変更が反映されることを確認

**タスクのステータス変更一覧**

各ステータスに変更して表示を確認：

| ステータス | 表示 |
|---|---|
| 未着手 | グレーのボーダー |
| 進行中 | 青のボーダー |
| 完了 | 緑のボーダー、タイトルに打ち消し線 |
| 保留 | 黄のボーダー |
| 中止 | 赤のボーダー |

**期限切れの確認**

1. タスクの期限を過去の日付に設定
2. WBSリストで `⚠` マークと赤文字で日付が表示されることを確認

---

#### 表示切り替え（WBSリスト ↔ テーブル）

1. プロジェクト詳細画面で「WBSリスト」「テーブル」ボタンを交互にクリック
2. テーブル表示ではフラットなリスト（親子関係なし）が表示されることを確認
3. WBSリストでは階層構造が表示されることを確認

**ステータスフィルター**

1. ステータスドロップダウンで「進行中」を選択
2. 進行中のタスクのみ表示されることを確認

---

#### 工数・進捗管理

1. 複数のタスクに予定工数・実績工数・進捗率を設定
2. プロジェクト詳細画面のヘッダー部分で以下が更新されることを確認：
   - 「予定: Xh」「実績: Xh」の合計値
   - プロジェクト進捗率のプログレスバー
   - 「完了タスク数/総タスク数」の表示

---

#### 今日のタスク（ToDo連携）

**タスクを今日のToDoに追加**

1. プロジェクト詳細の WBS ビューで任意のタスクにマウスオーバー
2. 右端の「☑」アイコン（半透明）をクリック
3. アイコンが黒く（不透明に）なることを確認
4. サイドバーの「今日のタスク」をクリック
5. 追加したタスクが表示されることを確認（プロジェクト名付き）

**今日のタスク画面の操作**

| 操作 | 手順 |
|---|---|
| **ステータス進行** | 左端の□ボタンをクリック（未着手→進行中→完了の順に切り替わる） |
| **明日に送る** | 「→明日」ボタン → タスクが一覧から消える |
| **今日のToDoから外す** | 「外す」ボタン → タスクが一覧から消える |

---

#### レスポンシブ表示の確認

**モバイル表示（スマホ幅）**

ブラウザの開発者ツール（F12）で画面幅を 375px に変更して確認：

- [ ] ヘッダーにハンバーガーメニュー（☰）が表示される
- [ ] ☰ タップでサイドバーが表示される
- [ ] サイドバー外をタップで閉じる
- [ ] プロジェクト一覧がカード形式で表示される
- [ ] WBSリストが横スクロールなしで表示される

**PC表示（1024px以上）**

- [ ] 左サイドバーが常時表示される
- [ ] プロジェクト一覧がテーブル形式で表示される
- [ ] WBSリストで工数・進捗バーなどの詳細カラムが表示される

---

#### 各画面のスクリーンショット撮影ポイント（記録用）

1. ダッシュボード（プロジェクトとタスクが複数ある状態）
2. プロジェクト一覧（複数プロジェクト、PC表示）
3. WBS ツリービュー（3階層、各ステータス混在）
4. テーブルビュー
5. 今日のタスク画面
6. タスク追加フォーム
7. モバイル表示（ハンバーガーメニュー）

---

## 5. よくあるトラブルと対処法

### `npm install` が失敗する

```bash
# npm キャッシュをクリアして再試行
npm cache clean --force
npm install
```

### ポート 3000 が使用中

```bash
# 別ポートで起動
npx next dev -p 3001
```

### データベースをリセットしたい

アプリを停止してから：

```bash
# ToDoアプリ
rm -rf todo-app/.data

# WBSアプリ
rm -rf wbs-app/.data
```

次回起動時にデータベースが自動再作成されます。

### ログインできない（401エラー）

- パスワードは6文字以上必要
- メールアドレスの入力ミスがないか確認
- データベースをリセットして新しいアカウントで登録し直す

### Playwright テストが失敗する

```bash
cd todo-app

# 開発サーバーが起動していることを確認
# （playwright.config.ts の webServer 設定で自動起動されるが）

# テストを1件ずつ実行して原因を特定
npx playwright test --reporter=list --headed

# テストデータが蓄積している場合、DBをリセット
rm -rf .data
```

### ビルドエラーが出る

```bash
# 型チェックのみ実行して原因を確認
npx tsc --noEmit

# ビルドキャッシュをクリア
rm -rf .next
npm run build
```

---

## 付録：データの保存場所

| アプリ | データベースファイル | 内容 |
|---|---|---|
| ToDoアプリ | `todo-app/.data/todos.db` | ユーザー・カテゴリ・ToDo |
| WBSアプリ | `wbs-app/.data/wbs.db` | ユーザー・プロジェクト・タスク・コメント |

どちらも SQLite のファイルです。DB Browser for SQLite（https://sqlitebrowser.org/）を使うと、GUI でデータを直接確認できます。
