# アプリ起動・テスト手順書

> この手順書は、プログラミング初心者の方でも迷わずアプリを起動・操作できるように書かれています。
> 見慣れない用語には補足を入れています。一つひとつ確認しながら進めてください。

---

## 目次

- [この手順書について](#この手順書について)
- [第1章：パソコンの準備](#第1章パソコンの準備)
- [第2章：ファイルをダウンロードする](#第2章ファイルをダウンロードする)
- [第3章：ToDoアプリを動かす](#第3章todoアプリを動かす)
- [第4章：WBS管理アプリを動かす](#第4章wbs管理アプリを動かす)
- [第5章：テストを自動で実行する（ToDoアプリ）](#第5章テストを自動で実行するtoDoアプリ)
- [第6章：困ったときの対処法](#第6章困ったときの対処法)

---

## この手順書について

### 2つのアプリについて

この手順書で扱うアプリは2つあります。

| アプリ名 | 何ができるか |
|---|---|
| **ToDoアプリ** | 日々のやることリストを管理するアプリ |
| **WBS管理アプリ** | プロジェクトのタスク・工数・進捗を管理するアプリ |

どちらも **ブラウザ（Chrome や Safari）で動く Web アプリ**です。  
スマホアプリのようにインストールは不要ですが、サーバーをパソコン上で起動する必要があります。

---

### 「ターミナル」とは

この手順書では「ターミナル」や「コマンドプロンプト」という言葉が出てきます。  
これは、**キーボードで文字を打ってパソコンに命令を送る画面**です。

| OS | 開き方 |
|---|---|
| **Mac** | `Command + スペース` で Spotlight を開き「ターミナル」と入力して Enter |
| **Windows** | スタートメニュー → 「Windows PowerShell」または「コマンドプロンプト」を検索して開く |

> **ターミナルで文字を入力して `Enter` キーを押すことを「コマンドを実行する」と言います。**  
> この手順書では `このような書体` で書かれたものがコマンドです。

---

## 第1章：パソコンの準備

### 1-1. Node.js をインストールする

**Node.js とは**：JavaScript（プログラミング言語）をパソコン上で動かすためのソフトウェアです。  
このアプリを動かすために必ず必要です。

#### すでにインストールされているか確認する

ターミナルを開いて、以下を入力して `Enter` を押してください。

```
node -v
```

このように表示されれば OK です（数字は多少違っても構いません）：

```
v20.11.0
```

`v20` 以上であればOK。`v18` や `v16` の場合はアップデートが必要です。  
`command not found` と表示された場合は、まだインストールされていません。次の手順に進んでください。

---

#### Mac の場合：Node.js のインストール手順

**方法1：公式サイトからインストール（推奨・簡単）**

1. ブラウザで https://nodejs.org/ja/ を開く
2. 「LTS（推奨版）」と書かれたボタンをクリックしてダウンロード
3. ダウンロードされた `.pkg` ファイルをダブルクリック
4. 画面の指示に従って「続ける」「インストール」をクリック
5. パスワードを求められたら Mac のログインパスワードを入力
6. インストール完了後、ターミナルを**再起動**（一度閉じて開き直す）
7. `node -v` を実行して `v20.x.x` と表示されれば成功

**方法2：Homebrew を使う（Mac ユーザーに人気）**

Homebrew とは Mac 用のパッケージ管理ツールです。すでにインストールされている方向けです。

```
brew install node
```

---

#### Windows の場合：Node.js のインストール手順

1. ブラウザで https://nodejs.org/ja/ を開く
2. 「LTS（推奨版）」と書かれたボタンをクリックしてダウンロード
3. ダウンロードされた `.msi` ファイルをダブルクリック
4. 「Next」を何度かクリックしてインストール完了まで進める  
   ※「Add to PATH」という項目が出たら必ずチェックを入れること
5. インストール完了後、PowerShell を**再起動**
6. `node -v` を実行して `v20.x.x` と表示されれば成功

---

### 1-2. インストール確認

ターミナルで以下の2つのコマンドを実行して、どちらも表示されることを確認してください。

```
node -v
```
→ `v20.x.x` のように表示される

```
npm -v
```
→ `10.x.x` のように表示される

> **npm とは**：Node.js と一緒にインストールされるツールです。アプリが使う外部ライブラリ（追加機能のパーツ）を管理します。

---

## 第2章：ファイルをダウンロードする

### 2-1. リポジトリをクローンする

「リポジトリ」とは GitHub（ソースコードの保管場所）にあるファイルのまとまりです。  
「クローン」とは、それを自分のパソコンにコピーすることです。

ターミナルで以下を実行してください：

```
git clone https://github.com/KaiOishii/AI-test-Claude.git
```

実行後、カレントディレクトリ（今いる場所）に `AI-test-Claude` というフォルダが作られます。

> **git が入っていない場合**  
> `git: command not found` と表示された場合は git をインストールしてください。  
> Mac: `brew install git` または Xcode Command Line Tools  
> Windows: https://git-scm.com/ からインストーラーをダウンロード

---

### 2-2. フォルダの中身を確認する

```
ls AI-test-Claude
```

以下のようなファイルが表示されれば正常です：

```
README.md  ToDoリストアプリ概要.md  todo-app/  wbs-app/  手順書.md
```

---

## 第3章：ToDoアプリを動かす

### 3-1. フォルダに移動する

ターミナルで `todo-app` フォルダに移動します。

```
cd AI-test-Claude/todo-app
```

> **`cd` とは**：「Change Directory（ディレクトリ移動）」の略です。  
> フォルダの中に入る操作です。

現在地を確認したい場合：
```
pwd
```
→ `.../AI-test-Claude/todo-app` のように表示されれば OK

---

### 3-2. 必要なパッケージをインストールする

```
npm install
```

> **`npm install` とは**：アプリが動くために必要な外部ライブラリを自動でダウンロードするコマンドです。  
> 初回のみ必要です。2回目以降は不要です。

完了まで1〜3分かかることがあります。最後に以下のような表示が出れば成功です：

```
added 321 packages in 45s
```

> ⚠️ `WARN` と書かれたメッセージが出ても問題ありません。`ERROR` と出た場合のみ第6章を参照してください。

---

### 3-3. 開発サーバーを起動する

```
npm run dev
```

以下のように表示されれば起動成功です：

```
▲ Next.js 16.x.x
- Local:   http://localhost:3000
- ready in 1234ms
```

> ⚠️ **このターミナルは閉じないでください。** 閉じるとアプリも止まります。  
> アプリを止めたいときは `Ctrl + C` を押します。

---

### 3-4. ブラウザでアクセスする

ブラウザ（Chrome・Safari・Edge など）を開き、アドレスバーに以下を入力して Enter：

```
http://localhost:3000
```

ログイン画面が表示されれば成功です。

---

### 3-5. アカウントを作る

1. ログイン画面の下にある「新規登録」リンクをクリック
2. 以下を入力します：
   - **名前**：何でも OK（例：テストユーザー）
   - **メールアドレス**：実在しなくても OK（例：test@example.com）
   - **パスワード**：6文字以上（例：password123）
3. 「登録する」ボタンをクリック
4. 自動的にメイン画面（`/todos`）に移動する

---

### 3-6. 各機能のテスト手順

#### ✅ ToDoを追加する

1. 右上または画面中の「**+ 追加**」ボタンをクリック
2. フォームが開く
3. 「タスク名を入力」という欄に `買い物に行く` と入力
4. 「**追加**」ボタンをクリック
5. リストに `買い物に行く` が表示されれば成功

---

#### ✅ ToDoを完了にする

1. 追加した `買い物に行く` の左端にある **□（チェックボックス）** をクリック
2. タイトルに **打ち消し線（取り消し線）** が入れば成功
3. もう一度クリックすると元に戻る

---

#### ✅ ToDoを編集する

1. `買い物に行く` にマウスカーソルを合わせる（スマホはタップ）
2. 右側に「**編集**」ボタンが現れる
3. 「編集」をクリック → フォームが開く
4. タイトルを `スーパーで買い物` に変更
5. 「**保存**」ボタンをクリック
6. タイトルが変わっていれば成功

---

#### ✅ ToDoを削除する

1. 削除したいToDoにマウスカーソルを合わせる
2. 「**削除**」ボタンをクリック
3. 一覧から消えれば成功

---

#### ✅ 優先度を設定する

1. 「**+ 追加**」をクリック
2. タイトルを入力
3. 優先度の選択欄で「**高**」を選ぶ
4. 「追加」
5. タスクの横に **赤色の「高」バッジ** が表示されれば成功

---

#### ✅ 検索する

1. 画面上部の検索欄（「タスクを検索...」と薄く書いてある欄）をクリック
2. 絞り込みたいキーワードを入力
3. 入力したキーワードを含むToDoだけが表示されれば成功
4. 検索欄をクリアすると全件表示に戻る

---

#### ✅ フィルターを使う

サイドバー（画面左）にフィルターボタンがあります：

| ボタン | 表示内容 |
|---|---|
| すべて | 全件表示 |
| 未完了 | まだ終わっていないToDoのみ |
| 完了済み | 完了したToDoのみ |

それぞれクリックして表示が絞り込まれることを確認してください。

---

#### ✅ カテゴリを追加する

1. サイドバーの「**カテゴリ**」という見出しの右にある「**+**」をクリック
2. カテゴリ名入力欄に `仕事` と入力
3. 「**追加**」ボタンをクリック
4. サイドバーに「仕事」が表示されれば成功
5. 「仕事」をクリックするとそのカテゴリのToDoのみ表示される（最初は0件）

---

#### ✅ サブタスクを追加する

1. 既存のToDoにマウスカーソルを合わせる
2. 「**＋**」ボタン（サブタスク追加アイコン）をクリック
3. サブタスクのタイトルを入力して「追加」
4. 親タスクの横に `▸ 0/1` のような展開ボタンが現れる
5. クリックするとサブタスクが表示される

---

#### ✅ ダークモードを使う

1. 画面右上の「**☾**」（月のマーク）ボタンをクリック
2. 画面全体が暗い色（ダークモード）に変わる
3. ページをリロード（F5 または Ctrl+R）してもダークモードのまま保たれていれば成功
4. 「**☀**」（太陽マーク）をクリックすると元に戻る

---

#### ✅ カレンダー表示に切り替える

1. サイドバーの「**カレンダー**」をクリック
2. 月次カレンダーが表示されれば成功
3. 「**<**」「**>**」ボタンで前月・翌月に移動できることを確認

---

#### ✅ ログアウトする

1. 画面右上の「**ログアウト**」をクリック
2. ログイン画面に戻れば成功
3. 再度ログインして元の画面に戻ることを確認

---

## 第4章：WBS管理アプリを動かす

### 4-1. 新しいターミナルウィンドウを開く

ToDoアプリのターミナルは **そのままにして**（閉じないで）、  
新しいターミナルウィンドウを開いてください。

> **なぜ新しいウィンドウ？**  
> ToDoアプリのサーバーが port 3000 で動いています。  
> WBSアプリも同じ port を使うため、先にToDoアプリを止めるか、別の port で起動します。  
> ここではToDoアプリを止めて WBS を 3000 番で動かします。
>
> 両方同時に動かしたい場合は第6章を参照してください。

ToDoアプリのターミナルで `Ctrl + C` を押してサーバーを止めてください。

---

### 4-2. wbs-app フォルダに移動する

**新しいターミナル**で以下を実行します：

```
cd AI-test-Claude/wbs-app
```

確認：
```
pwd
```
→ `.../AI-test-Claude/wbs-app` と表示されれば OK

---

### 4-3. パッケージをインストールする

```
npm install
```

完了まで1〜3分待ちます。

---

### 4-4. 開発サーバーを起動する

```
npm run dev
```

以下が表示されれば起動成功：

```
▲ Next.js 16.x.x
- Local:   http://localhost:3000
```

---

### 4-5. ブラウザでアクセスする

```
http://localhost:3000
```

ログイン画面が表示されれば成功。

---

### 4-6. アカウントを作る

1. 「新規登録」をクリック
2. 入力：
   - 名前（例：テスト太郎）
   - メールアドレス（例：test@example.com）
   - パスワード（6文字以上）
3. 「登録する」
4. ダッシュボード（ホーム画面）に移動すれば成功

---

### 4-7. プロジェクトを作成する

WBSアプリでは「**プロジェクト**」の中に「**タスク**」を作ります。  
まずプロジェクトを作りましょう。

1. 左サイドバーの「**プロジェクト**」をクリック
2. 右上の「**+ 新規**」ボタンをクリック
3. フォームに入力します：
   - **プロジェクト名**：`WBS管理アプリ作成`
   - **説明**：`テスト用のプロジェクトです`
   - **開始日**：今日の日付
   - **終了予定日**：1ヶ月後の日付
   - **ステータス**：進行中
   - **進捗率**：0
4. 「追加」をクリック
5. 一覧に `WBS管理アプリ作成` が表示されれば成功

---

### 4-8. WBSタスクを作成する（1階層目）

1. プロジェクト名「**WBS管理アプリ作成**」をクリック
2. プロジェクト詳細画面に移動する
3. 右上の「**+ タスク追加**」をクリック
4. 入力：
   - **タスク名**：`要件定義`
   - **ステータス**：未着手
   - **優先度**：高
   - **期限**：1週間後の日付
   - **予定工数(h)**：8
5. 「追加」をクリック
6. WBSリストに `要件定義` が表示されれば成功

同様に2つ目のタスクを追加：
- タスク名：`設計`、優先度：中、予定工数：12

---

### 4-9. 子タスクを追加する（2階層目）

親タスクの下に子タスク（サブタスク）を作ります。

1. `要件定義` タスクにマウスカーソルを合わせる
2. 右端に「**＋**」ボタンが現れる → クリック
3. 子タスクの追加フォームが開く
4. 入力：
   - **タスク名**：`画面要件の定義`
   - **予定工数**：3
5. 「追加」
6. `要件定義` の左側に「**▾**」ボタンが現れ、その下に `画面要件の定義` が表示される

同様に `要件定義` の子タスクをもう1つ追加：
- タスク名：`機能要件の定義`、予定工数：5

---

### 4-10. ツリーの展開・折りたたみ

1. `▾` ボタンをクリック → 子タスクが**非表示**になり `▸` に変わる
2. `▸` をクリック → 子タスクが**再表示**される

---

### 4-11. さらに下の階層を追加する（3階層目）

1. `画面要件の定義` にマウスオーバー → 「＋」をクリック
2. タスク名：`ログイン画面の設計`、予定工数：2 を追加
3. `要件定義` > `画面要件の定義` > `ログイン画面の設計` の3段階構造になれば成功

---

### 4-12. タスクのステータスを変更する

1. `要件定義` にマウスオーバー → 「**編集**」をクリック
2. ステータスを「**進行中**」に変更 → 「保存」
3. タスクのステータスバッジが「**進行中**（青）** に変わることを確認

各ステータスの色確認：

| ステータス | バッジの色 |
|---|---|
| 未着手 | グレー |
| 進行中 | 青 |
| 完了 | 緑（タイトルに打ち消し線） |
| 保留 | 黄 |
| 中止 | 赤 |

---

### 4-13. 工数・進捗率を設定する

1. `要件定義` → 「編集」
2. **実績工数**：4 を入力
3. **進捗率**：50 を入力
4. 「保存」
5. プロジェクト詳細画面のヘッダー部分で以下が変わることを確認：
   - 「実績: 4h」
   - 進捗バーが 50% 付近になる

---

### 4-14. 表示切り替えをテストする

プロジェクト詳細画面の上部にある切り替えボタン：

- 「**WBSリスト**」→ 階層ツリー表示（デフォルト）
- 「**テーブル**」→ 全タスクをフラットな表で表示

どちらも正しく表示されることを確認してください。

---

### 4-15. 今日のタスク（ToDo連携）を使う

WBSのタスクを「今日やること」として登録できます。

1. プロジェクト詳細 WBSビューで任意のタスクにマウスオーバー
2. 右端の「**☑**」アイコン（薄く表示）をクリック
3. アイコンが黒く（濃く）なれば登録成功
4. 左サイドバーの「**今日のタスク**」をクリック
5. 追加したタスクがプロジェクト名付きで表示されれば成功

#### 今日のタスク画面での操作

| 操作 | 方法 | 説明 |
|---|---|---|
| ステータスを進める | 左端の □ ボタンをクリック | 未着手→進行中→完了の順に変わる |
| 明日に送る | 「→明日」ボタン | 翌日のToDoとして移動する |
| 今日から外す | 「外す」ボタン | 今日のタスクリストから除外する |

---

### 4-16. ダッシュボードを確認する

サイドバーの「**ホーム**」をクリック。  
以下が表示されていれば成功：

- 「今日のタスク」欄：登録したタスクが表示される
- 「進行中のプロジェクト」欄：作成したプロジェクトが表示される

---

### 4-17. スマホ表示を確認する（任意）

ブラウザの開発者ツールを使って、スマホ幅で見ることができます。

**Chrome の場合：**
1. `F12`（Mac は `Command + Option + I`）で開発者ツールを開く
2. ツールバーの「スマホアイコン（□に矢印のマーク）」をクリック
3. 画面上部のプルダウンで「iPhone SE」などを選択
4. ページをリロード

確認ポイント：
- [ ] 左上に「**☰**」（ハンバーガーメニュー）ボタンが表示される
- [ ] ☰ をタップするとサイドバーが現れる
- [ ] サイドバー外をタップすると閉じる

---

## 第5章：テストを自動で実行する（ToDoアプリ）

ToDoアプリには「**Playwright**」という自動テストツールが組み込まれています。  
テストを実行すると、ブラウザを自動で操作して全機能が正常に動くかチェックしてくれます。

### 5-1. ToDoアプリのフォルダに移動する

```
cd AI-test-Claude/todo-app
```

### 5-2. 開発サーバーを起動する（すでに動いていれば不要）

別のターミナルで：
```
npm run dev
```

起動確認後、次の手順へ。

### 5-3. テストを実行する

テストを実行するターミナルで：

```
npx playwright test --reporter=list
```

> **実行中に何が起きる？**  
> 見えないブラウザが自動で開いて操作します。  
> ログインしたり、ToDoを追加したり、削除したりを自動で繰り返します。  
> 30秒〜1分で全テストが完了します。

### 5-4. 結果を確認する

成功した場合：

```
Running 13 tests using 1 worker

  ✓  1 tests/todo.spec.ts:29:7 › 認証フロー › 未認証時はloginにリダイレクト
  ✓  2 tests/todo.spec.ts:35:7 › 認証フロー › ログイン成功後にメイン画面へ遷移
  ✓  3 tests/todo.spec.ts:41:7 › 認証フロー › ログアウト後にlogin画面へ遷移
  ✓  4 tests/todo.spec.ts:54:7 › ToDoの基本操作 › ToDoの追加
  ✓  5 tests/todo.spec.ts:61:7 › ToDoの基本操作 › ToDoの完了切り替え
  ✓  6 tests/todo.spec.ts:70:7 › ToDoの基本操作 › ToDoの削除
  ✓  7 tests/todo.spec.ts:80:7 › ToDoの基本操作 › ToDoの編集
  ✓  8 tests/todo.spec.ts:100:7 › 検索機能 › タイトルで検索できる
  ✓  9 tests/todo.spec.ts:116:7 › 優先度機能 › 高優先度のToDoを追加できる
  ✓ 10 tests/todo.spec.ts:132:7 › フィルター機能 › 未完了フィルターで完了済みが非表示
  ✓ 11 tests/todo.spec.ts:151:7 › カテゴリ管理 › カテゴリの追加
  ✓ 12 tests/todo.spec.ts:165:7 › ダークモード › ダークモード切り替えボタンが存在する
  ✓ 13 tests/todo.spec.ts:177:7 › カレンダー表示 › カレンダービューに切り替えられる

  13 passed (xx.xs)
```

`13 passed` と表示されれば全テスト成功です。

### 5-5. 失敗した場合

`✘`（×印）が付いているテストがあれば失敗です。  
失敗した場合は以下を試してください：

```bash
# データベースをリセットしてからもう一度
rm -rf .data
npx playwright test --reporter=list
```

### 5-6. テスト結果をレポートで見る（任意）

```
npx playwright show-report
```

ブラウザが自動で開き、テスト結果のきれいなレポートが表示されます。  
各テストの実行時間や成否、失敗時のスクリーンショットなどが確認できます。

---

## 第6章：困ったときの対処法

### ❓ `npm install` でエラーが出た

```
npm cache clean --force
npm install
```

キャッシュをクリアしてからやり直します。

---

### ❓ `http://localhost:3000` を開いてもアプリが表示されない

ターミナルで `npm run dev` が動いているか確認してください。  
止まっていたら再度 `npm run dev` を実行します。

---

### ❓ ポート 3000 が使用中と言われた（Address already in use）

別のアプリが 3000 番を使っています。別のポート番号で起動します：

```
npx next dev -p 3001
```

その場合のアクセス先は：
```
http://localhost:3001
```

---

### ❓ ToDoアプリと WBS アプリを同時に動かしたい

**ターミナルを2つ開いて**、それぞれ別のポートで起動します。

**ターミナル1（ToDoアプリ）：**
```
cd AI-test-Claude/todo-app
npm run dev
```
→ http://localhost:3000 でアクセス

**ターミナル2（WBSアプリ）：**
```
cd AI-test-Claude/wbs-app
npx next dev -p 3001
```
→ http://localhost:3001 でアクセス

---

### ❓ ログインできない・パスワードを忘れた

データベースをリセットして新しいアカウントを作り直します。  
**アプリを停止**（`Ctrl + C`）してから：

```bash
# ToDoアプリのデータをリセット
rm -rf AI-test-Claude/todo-app/.data

# WBSアプリのデータをリセット
rm -rf AI-test-Claude/wbs-app/.data
```

> ⚠️ リセットするとすべてのデータが消えます。

---

### ❓ `git clone` でエラーが出た・git が使えない

**Mac の場合：**

ターミナルで `git` と入力して Enter。  
「コマンドラインデベロッパーツールをインストールしますか？」というダイアログが出たら「インストール」をクリック。

または：
```
brew install git
```

**Windows の場合：**

https://git-scm.com/ にアクセスして、「Download for Windows」からインストーラーをダウンロードして実行。

---

### ❓ `command not found: node` と出る

Node.js がインストールされていないか、パスが通っていません。

1. 第1章の「Node.js をインストールする」を再度確認
2. インストール後、ターミナルを**完全に閉じて再起動**
3. `node -v` を再度実行

---

### ❓ テストが全部失敗する

開発サーバーが起動していない可能性があります。

1. 別のターミナルで `npm run dev` が動いていることを確認
2. http://localhost:3000 にブラウザでアクセスして画面が表示されることを確認
3. 確認できたらテストを再実行

---

## 付録

### データの保存場所

アプリのデータは以下のファイルに保存されます（SQLite データベース）：

| アプリ | ファイルの場所 |
|---|---|
| ToDoアプリ | `AI-test-Claude/todo-app/.data/todos.db` |
| WBSアプリ | `AI-test-Claude/wbs-app/.data/wbs.db` |

このファイルは通常 `.gitignore` により Git 管理対象外です。  
データを視覚的に確認したい場合は「**DB Browser for SQLite**」というアプリが便利です。  
https://sqlitebrowser.org/ から無料でダウンロードできます。

---

### よく使うコマンド一覧

| コマンド | 意味 |
|---|---|
| `cd フォルダ名` | そのフォルダに移動する |
| `ls` （Mac）/ `dir` （Windows） | 今いる場所のファイル一覧を表示 |
| `pwd` | 今いる場所（パス）を表示 |
| `npm install` | パッケージをインストール（初回のみ） |
| `npm run dev` | 開発サーバーを起動 |
| `Ctrl + C` | 実行中のコマンドを止める |
| `npx playwright test` | 自動テストを実行 |
